[phases.setup]
aptPkgs = ["default-libmysqlclient-dev", "pkg-config", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python setup_database.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
